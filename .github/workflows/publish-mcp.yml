name: Publish to MCP Registry

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Install mcp-publisher
        run: |
          curl -L "https://github.com/modelcontextprotocol/registry/releases/latest/download/mcp-publisher_$(uname -s | tr '[:upper:]' '[:lower:]')_$(uname -m | sed 's/x86_64/amd64/;s/aarch64/arm64/').tar.gz" | tar xz mcp-publisher

      - name: Validate server.json
        run: ./mcp-publisher validate

      # Namespace com.shortpixel.mcp/* is proven by HTTP auth on mcp.shortpixel.com
      # Host the public key at https://mcp.shortpixel.com/.well-known/mcp-registry-auth
      # Add repo secret MCP_PRIVATE_KEY (Ed25519 private key hex)
      # https://modelcontextprotocol.io/registry/authentication#http-authentication
      - name: Authenticate to MCP Registry
        run: ./mcp-publisher login http --domain mcp.shortpixel.com --private-key "${{ secrets.MCP_PRIVATE_KEY }}"

      - name: Sync version from git tag
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          VERSION="${GITHUB_REF#refs/tags/v}"
          jq --arg v "$VERSION" '.version = $v' server.json > server.tmp && mv server.tmp server.json

      - name: Publish server to MCP Registry
        run: ./mcp-publisher publish
